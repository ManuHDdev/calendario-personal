name: CI/CD Juegos

on:
  push:
    branches:
      - main
    paths:
      - 'juegos/**'
      - '.github/workflows/juegos-ci.yml'

jobs:
  # ─── 1. Build backend ────────────────────────────────────────────────────────
  lint-and-build-backend:
    name: Juegos · Build backend
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install & build
        working-directory: juegos/backend
        run: npm ci && npm run build

      - name: Run tests
        working-directory: juegos/backend
        run: npm test

  # ─── 2. Build frontend ───────────────────────────────────────────────────────
  lint-and-build-frontend:
    name: Juegos · Build frontend
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install & build
        working-directory: juegos/frontend
        run: npm ci && npm run build

  # ─── 3. Docker build & push to GHCR ─────────────────────────────────────────
  docker-build-and-push:
    name: Juegos · Docker build & push
    runs-on: ubuntu-latest
    needs: [lint-and-build-backend, lint-and-build-frontend]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set lowercase owner
        run: echo "OWNER=$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build & push juegos-backend
        uses: docker/build-push-action@v5
        with:
          context: ./juegos/backend
          push: true
          tags: |
            ghcr.io/${{ env.OWNER }}/juegos-backend:latest
            ghcr.io/${{ env.OWNER }}/juegos-backend:${{ github.sha }}

      - name: Build & push juegos-frontend
        uses: docker/build-push-action@v5
        with:
          context: ./juegos/frontend
          push: true
          tags: |
            ghcr.io/${{ env.OWNER }}/juegos-frontend:latest
            ghcr.io/${{ env.OWNER }}/juegos-frontend:${{ github.sha }}

  # ─── 4. Deploy al VPS ────────────────────────────────────────────────────────
  deploy:
    name: Deploy · Juegos VPS
    runs-on: ubuntu-latest
    needs: docker-build-and-push

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Copiar docker-compose al VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: 87.216.88.165
          port: 2269
          username: manu
          key: ${{ secrets.VPS_SSH_KEY }}
          source: "juegos/infra/docker-compose.prod.yml"
          target: /home/manu/juegos
          strip_components: 2

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 87.216.88.165
          port: 2269
          username: manu
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -e

            cd /home/manu/juegos

            docker compose -f docker-compose.prod.yml pull juegos-backend juegos-frontend
            docker compose -f docker-compose.prod.yml up -d --no-deps juegos-backend juegos-frontend
            docker compose -f docker-compose.prod.yml ps
